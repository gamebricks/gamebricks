udefine(['gameboard/assetloader', 'gameboard/input', 'gameboard/loop', 'gameboard/log', 'gameboard/timer'], function(AssetLoader, Input, Loop, Log, Timer) {
  
  return {
    AssetLoader: AssetLoader,
    Input: Input,
    Loop: Loop,
    Log: Log,
    Timer: Timer
  };
  
});
